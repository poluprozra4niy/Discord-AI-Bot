# 🎨 Быстрый старт с Glitch

## Шаг 1: Создайте GitHub репозиторий

1. Перейдите: https://github.com/new
2. Название: `discord-dnd-bot`
3. **Public** (или  Private)
4. **НЕ** добавляйте README, .gitignore (у нас уже есть)
5. **Create repository**

## Шаг 2: Загрузите код на GitHub

В PowerShell (в папке с ботом):

```powershell
# Инициализация (если еще не сделано)
git init

# Добавить все файлы
git add .

# Коммит
git commit -m "Prepared for Glitch deployment"

# Подключить удаленный репозиторий (ЗАМЕНИТЕ на ваш!)
git remote add origin https://github.com/ВАШ_USERNAME/discord-dnd-bot.git

# Отправить на GitHub
git branch -M main
git push -u origin main
```

## Шаг 3: Glitch

1. Перейдите: https://glitch.com/
2. **Sign Up** → **Sign up with GitHub**
3. **New Project** → **Import from GitHub**
4. Вставьте: `https://github.com/ВАШ_USERNAME/discord-dnd-bot`
5. **OK**

Glitch начнет импорт и установку зависимостей!

## Шаг 4: Настройка .env в Glitch

1. В левой панели найдите `.env` файл
2. Кликните на него
3. Добавьте ваши секреты:

```env
DISCORD_TOKEN=ваш_токен_здесь
CLIENT_ID=ваш_client_id
OPENAI_API_KEY=ваш_openai_key_если_есть
GLITCH=true
```

(**ВАЖНО:** `GLITCH=true` - чтобы использовать `.data` вместо `data`)

## Шаг 5: Создание папок для данных

В терминале Glitch (внизу, кнопка **Terminal**):

```bash
mkdir -p .data/characters
mkdir -p .data/sessions
ls -la .data/
```

## Шаг 6: Регистрация команд Discord

В терминале Glitch:

```bash
node deploy-commands.js
```

Должно показать:
```
✅ Загружена команда: roll
✅ Загружена команда: character
...
Successfully registered application commands.
```

## Шаг 7: Проверка логов

1. **Tools** → **Logs**
2. Должны увидеть:

```
🎲 D&D AI Bot запущен!
📝 Бот: YourBot#1234
🌐 HTTP server running on port 3000
```

✅ Бот работает!

## Шаг 8: Настройка UptimeRobot (чтобы не засыпал)

1. Перейдите: https://uptimerobot.com/
2. **Sign Up** (бесплатно)
3. **Add New Monitor**
4. Настройки:
   - **Monitor Type:** HTTP(s)
   - **Friendly Name:** Discord D&D Bot
   - **URL:** скопируйте из Glitch (кнопка **Share** → **Live App**)
     - Будет типа: `https://ваш-проект.glitch.me`
   - **Monitoring Interval:** 5 minutes
5. **Create Monitor**

✅ Теперь бот НЕ заснет! UptimeRobot будет пинговать каждые 5 минут!

## Шаг 9: Проверка

1. Откройте Discord
2. Попробуйте `/character create`
3. Создайте персонажа
4. `/character list` - должен показать!

## Шаг 10: Проверка постоянства данных

В терминале Glitch:

```bash
# Посмотреть на данные
ls -la .data/characters/

# Перезапустить проект
refresh

# Снова проверить
ls -la .data/characters/
```

Файлы должны остаться! ✅

---

## 🎉 Готово!

Ваш бот теперь:
- ✅ Работает 24/7 на Glitch
- ✅ БЕЗ кредитной карты
- ✅ С постоянным хранилищем (`.data/`)
- ✅ Не засыпает (UptimeRobot)

## Полезные ссылки:

- Ваш проект: https://glitch.com/edit/#!/ваш-проект
- Live App: https://ваш-проект.glitch.me  
- UptimeRobot: https://uptimerobot.com/dashboard

## При обновлении кода:

```powershell
# На компьютере
git add .
git commit -m "Update"
git push

# В Glitch: Tools → Terminal
git pull origin main
refresh
```

Удачи! 🎲⚔️🐉
