#!/bin/bash

# Цвета для вывода
GREEN='\033[0;32m'
BLUE='\033[0;34m'
YELLOW='\033[1;33m'
NC='\033[0m' # No Color

echo -e "${BLUE}🔄 Начинаю обновление Discord D&D Bot...${NC}"

# Переходим в директорию проекта
cd ~/discord-dnd-bot || { echo "❌ Ошибка: директория проекта не найдена"; exit 1; }

# Получаем последние изменения
echo -e "${YELLOW}📥 Получение обновлений с GitHub...${NC}"
git pull origin main

if [ $? -ne 0 ]; then
    echo "❌ Ошибка при получении обновлений"
    exit 1
fi

# Обновляем зависимости
echo -e "${YELLOW}📦 Обновление зависимостей...${NC}"
npm install

# Перерегистрируем команды Discord
echo -e "${YELLOW}🔧 Регистрация команд Discord...${NC}"
npm run deploy

# Перезапускаем бота через PM2
echo -e "${YELLOW}♻️  Перезапуск бота...${NC}"
pm2 restart dnd-bot

# Показываем статус
echo -e "${GREEN}✅ Бот успешно обновлен и перезапущен!${NC}"
pm2 status dnd-bot
